{
  "name": "zenkai",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "wrangler dev worker/index.js",
    "worker:deploy": "wrangler deploy",
    "worker:db:init": "wrangler d1 execute zenkai --file=worker/schema.sql",
    "worker:db:init:remote": "wrangler d1 execute zenkai --remote --file=worker/schema.sql"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "wrangler": "^3.101.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "g": "^2.0.1",
    "jsonwebtoken": "^9.0.3"
  }
}
